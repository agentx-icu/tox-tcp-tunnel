mode: server
data_dir: /var/lib/toxtunnel

service:
  auto_start: true
  allow_client_daemon: false

logging:
  level: info
  file: /var/lib/toxtunnel/toxtunnel.log

tox:
  udp_enabled: true
  tcp_port: 33445
  bootstrap_mode: auto

server:
  # Add access-control settings here if you want to restrict what friends can reach.
